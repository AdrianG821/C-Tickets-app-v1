<Page x:Class="TicketsProject.TicketsList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TicketsProject"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TicketsList">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0">
                <TextBlock FontSize="22" FontWeight="Bold">Support App</TextBlock>
                <TextBlock FontSize="15">Manage your tickets</TextBlock>
            </StackPanel>
            <Button Click="AddNewTicket" Grid.Column="1" Content="Add new ticket" VerticalAlignment="Center" HorizontalAlignment="Left" Padding="5"/>
            <Button Click="AddNewClient" Content="Add Client" Width="100" Padding="5" Margin="362,13,271,13"/>
            <Button Click="AddNewTester" Content="Add tester" Width="100" Padding="5" Margin="478,12,144,14"/>
            <Button Click="AddNewDeveloper" Content="Add developer" Width="100" Padding="5" Margin="597,13,23,9" Height="32"/>

        </Grid>


        <StackPanel Grid.Row="1">
            <WrapPanel >
                <TextBox x:Name="IdSearchBox" Width="75" MaxLength="10"></TextBox>
                <ComboBox x:Name="StatusDropDown" Width="150" SelectionChanged="FilterTickets">
                </ComboBox>

                <ComboBox x:Name="PriorityDropDown" Width="150" SelectionChanged="FilterTickets" >
                </ComboBox>
                <Button Click="FiltersSearch" Content="Search" Width="100" Height="20"/>
            </WrapPanel>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock x:Name="NameTextBlock"/>

            <Button Click="RefreshTickets" Content="Refresh" Width="100" Height="20" HorizontalAlignment="Right"/>

            <ListView x:Name="ListViewTicket" ItemsSource="{Binding Tickets}" MouseDoubleClick="SelectTicket">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding TicketsId}"/>
                        <GridViewColumn Header="Tickets Name" DisplayMemberBinding="{Binding TicketsName}"/>
                        <GridViewColumn Header="Priority" DisplayMemberBinding="{Binding TicketsPriority}"/>
                        <GridViewColumn Header="Tester" DisplayMemberBinding="{Binding TicketsTester}"/>
                        <GridViewColumn Header="Developer" DisplayMemberBinding="{Binding TicketsDeveloper}"/>
                        <GridViewColumn Header="Client" DisplayMemberBinding="{Binding TicketsClient}"/>
                        <GridViewColumn Header="Status" DisplayMemberBinding="{Binding TicketsStatus}"/>
                    </GridView>
                </ListView.View>
            </ListView>

        </StackPanel>
    </Grid>

</Page>
